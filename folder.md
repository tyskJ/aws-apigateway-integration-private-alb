# フォルダ構成

- フォルダ構成は以下の通り

```
.
└── envs
    ├── config
    │   ├── dummy
    │   └── apigateway-parameter.yml      APIGATEWAY スタック用パラメータファイル (Ignore Git)
    └── apigateway.yml                    APIGATEWAY スタック定義ファイル
```
